security:
    providers:
        app_user_provider:
            entity:
                class: App\Entity\User
                property: email

    firewalls:
        admin:
            pattern: ^/admin
            provider: app_user_provider
            lazy: true

    access_control:
        - { path: ^/admin, roles: ROLE_ADMIN }
